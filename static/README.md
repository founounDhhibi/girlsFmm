# Static Assets

Frontend assets used by the Flask templates.

## Files

- `style.css`: Main UI styling, dashboard cards, adversarial alert styles.
- `js/main.js`: Analyzer flow, dashboard refresh, audit log rendering, simulation runner.

## Conventions

- Keep selectors stable for template IDs used by JavaScript.
- Avoid changing API response key names without updating `main.js`.
